{%- style -%}
  .about {
    padding: 6rem 0;
    background: var(--color-background);
  }

  .about__wrapper {
    max-width: var(--spacing-page-width);
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .about__content {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .about__title {
    font-size: var(--font-size-3xl);
    color: var(--color-foreground);
    margin-bottom: 1rem;
  }

  .about__description {
    color: var(--color-muted-foreground);
    line-height: var(--line-height-relaxed);
  }

  .about__values {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    margin-top: 2rem;
  }

  .value-card {
    padding: 1.5rem;
    background: var(--color-secondary);
    border-radius: 0.5rem;
  }

  .value-card__title {
    font-size: var(--font-size-lg);
    color: var(--color-secondary-foreground);
    margin-bottom: 0.5rem;
  }

  .value-card__description {
    color: var(--color-muted-foreground);
    line-height: var(--line-height-relaxed);
  }

  .about__cta {
    margin-top: 2rem;
  }

  .about__cta-button {
    display: inline-block;
    padding: 1rem 2rem;
    background: var(--color-primary);
    color: var(--color-primary-foreground);
    text-decoration: none;
    border-radius: 0.375rem;
    font-weight: 500;
    transition: background-color 0.2s ease;
  }

  .about__cta-button:hover {
    background: var(--color-ring);
  }

  .about__image {
    position: relative;
    border-radius: 0.5rem;
    overflow: hidden;
  }

  .about__image img {
    width: 100%;
    height: auto;
    display: block;
  }

  .about__image-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      to bottom,
      rgba(0, 0, 0, 0.1),
      rgba(0, 0, 0, 0.2)
    );
  }

  @media screen and (max-width: 1024px) {
    .about__wrapper {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .about__image {
      order: -1;
    }
  }

  @media screen and (max-width: 768px) {
    .about {
      padding: 4rem 0;
    }

    .about__title {
      font-size: var(--font-size-2xl);
    }

    .about__values {
      grid-template-columns: 1fr;
    }
  }
{%- endstyle -%}

<div class="about">
  <div class="about__wrapper">
    <div class="about__content">
      <div>
        <h2 class="about__title">{{ section.settings.title }}</h2>
        <div class="about__description">
          {{ section.settings.description }}
        </div>
      </div>

      <div class="about__values">
        {%- for block in section.blocks -%}
          <div class="value-card" {{ block.shopify_attributes }}>
            <h3 class="value-card__title">{{ block.settings.title }}</h3>
            <p class="value-card__description">{{ block.settings.description }}</p>
          </div>
        {%- endfor -%}
      </div>

      {%- if section.settings.button_text != blank and section.settings.button_link != blank -%}
        <div class="about__cta">
          <a href="{{ section.settings.button_link }}" class="about__cta-button">
            {{ section.settings.button_text }}
          </a>
        </div>
      {%- endif -%}
    </div>

    <div class="about__image">
      {%- if section.settings.image != blank -%}
        <img
          src="{{ section.settings.image | image_url: width: 800 }}"
          alt="{{ section.settings.image.alt | escape }}"
          width="800"
          height="600"
          loading="lazy"
        >
      {%- endif -%}
      <div class="about__image-overlay"></div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "About",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "About James Hardware"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description",
      "default": "<p>For over 50 years, James Hardware has been providing quality hardware solutions to businesses across the country. Our commitment to excellence and customer service has made us a trusted partner in the industry.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Learn More About Us"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    }
  ],
  "blocks": [
    {
      "type": "value",
      "name": "Value",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Our Value"
        },
        {
          "type": "text",
          "id": "description",
          "label": "Description",
          "default": "Brief description of this value"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "About",
      "blocks": [
        {
          "type": "value",
          "settings": {
            "title": "Quality",
            "description": "We never compromise on quality"
          }
        },
        {
          "type": "value",
          "settings": {
            "title": "Service",
            "description": "Customer satisfaction is our priority"
          }
        },
        {
          "type": "value",
          "settings": {
            "title": "Expertise",
            "description": "50+ years of industry experience"
          }
        },
        {
          "type": "value",
          "settings": {
            "title": "Innovation",
            "description": "Always evolving with the industry"
          }
        }
      ]
    }
  ]
}
{% endschema %} 